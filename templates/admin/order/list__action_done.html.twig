{# @var \App\Admin\AbstractAdmin admin #}
{# @var \App\Entity\Order object #}

{% if object.status == 'open' and admin.hasAccess('done', object) and admin.hasRoute('done') %}
    {% set button_content = sonata_config.getOption('list_action_button_content') %}
    <a
        href="{{ admin.generateObjectUrl('done', object, actions.link_parameters|default([])) }}"
        class="btn btn-sm btn-success done_link"
        title="{{ 'ORDER_ENTITY.BUTTON.DONE'|trans({}, 'SonataAdminBundle') }}"
    >
        {% if button_content == 'icon' or button_content == 'all' %}
            <i class="fas fa-edit" aria-hidden="true"></i>
        {% endif %}
        {% if button_content == 'text' or button_content == 'all' %}
            {{ 'ORDER_ENTITY.BUTTON.DONE'|trans({}, 'SonataAdminBundle') }}
        {% else %}
            <span class="sr-only">
                {{ 'ORDER_ENTITY.BUTTON.DONE'|trans({}, 'SonataAdminBundle') }}
            </span>
        {% endif %}
    </a>
{% endif %}
