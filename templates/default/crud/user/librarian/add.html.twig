{% extends 'layout.main.html.twig' %}


{% block breadcrumbs %}
    {% set breadcrumbs = [
        {'url': path('crud.librarian.list'), 'title': 'Librarians'},
        {'url': path('crud.librarian.add'), 'title': 'Add', 'active': true},
    ] %}
    {{ parent() }}
{% endblock %}

{% block content %}

    <form action="{{ path('crud.librarian.add') }}" method="post" novalidate="novalidate">
        {{ form_rest(form) }}
        <button type="submit" class="btn btn-success">Submit</button>
    </form>
{% endblock %}

