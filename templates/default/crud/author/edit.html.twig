{% extends 'layout.main.html.twig' %}


{% block breadcrumbs %}
    {% set breadcrumbs = [
        {'url': path('crud.author.list'), 'title': 'Authors'},
        {'url': path('crud.author.edit', {'id': id}), 'title': 'Edit', 'active': true},
    ] %}
    {{ parent() }}
{% endblock %}

{% block content %}

    <form action="{{ path('crud.author.edit', {'id': id}) }}" method="post" novalidate="novalidate">
        {{ form_rest(form) }}
        <button type="submit" class="btn btn-success">Submit</button>
    </form>
{% endblock %}

