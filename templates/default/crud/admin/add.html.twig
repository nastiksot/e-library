{% extends 'layout.main.html.twig' %}


{% block breadcrumbs %}
    {% set breadcrumbs = [
        {'url': path('admin.list'), 'title': 'Admins'},
        {'url': path('admin.add'), 'title': 'Add', 'active': true},
    ] %}
    {% include 'default/_partials/breadcrumbs.html.twig' %}
{% endblock %}

{% block content %}

    <form action="{{ path('admin.add') }}" method="post" novalidate="novalidate">
        {{ form_rest(form) }}
        <button type="submit" class="btn btn-success">Submit</button>
    </form>
{% endblock %}

