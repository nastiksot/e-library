/* global window */
'use strict';

let $ = window.$;
$(document).ready(() => {
    // let $final = $('.decision-final');
    // let $positive = $('.decision-positive');
    //
    // $final.on('change', () => {
    //     if (Number($final.val()) === 0) {
    //         //$positive.find('option:selected').removeAttr('selected');
    //         //$positive.find('option[value=0]').attr('selected', 0);
    //         $positive.val('0').trigger('change');
    //     }
    // });
});

