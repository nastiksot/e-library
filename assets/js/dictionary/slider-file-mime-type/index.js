"use strict";

/**
 * @readonly
 * @enum {NULL|String}
 */
const SLIDER_FILE_MIME_TYPE = Object.freeze({
    IMAGE: "image",
    VIDEO: "video",
    ANIMATED_GIF: "animated_gif",
});

export default SLIDER_FILE_MIME_TYPE;
